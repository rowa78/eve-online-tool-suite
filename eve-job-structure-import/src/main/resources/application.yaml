spring:
  application:
    name: eve-job-structure-import
  main:
    web-application-type: none

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO

structures:
  file: structures_markets.txt